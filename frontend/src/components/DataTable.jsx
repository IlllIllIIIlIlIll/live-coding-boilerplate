/**
 * TODO: implementasikan tabel generik yang dipakai ulang di halaman
 * Unit, Tagihan, dan Pemilik.
 * columns: [{ key, label, render? }]
 * rows: array of object
 * actions?: (row) => ReactNode -> kolom aksi opsional (edit/hapus), untuk admin
 * Gunakan class .data-table dari index.css.
 */
export default function DataTable({ columns, rows, actions }) {
  return null;
}
